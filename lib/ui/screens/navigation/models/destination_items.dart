import 'package:flutter/material.dart';

class DestinationItems {
  final int index;
  final String title;
  final IconData icon;
  final Color color;
  final Color activeColor;
  const DestinationItems(
      this.index, this.title, this.icon, this.color, this.activeColor);
}
